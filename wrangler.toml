name = "marketwatchpro"
compatibility_date = "2025-10-10"

# 🧱 Static frontend build output (Vite)
# Your frontend lives in client/src, so make sure your Vite build outputs to this directory.
# You may need to configure Vite to output to `dist/public` if it's not already.
[assets]
directory = "./client/src"

# 🌐 Deployment environment variables
# Note: Database credentials (DATABASE_URL, SUPABASE_URL, etc.) should be set via Cloudflare dashboard
# for security. Only non-sensitive configs are listed here.
[vars]
NODE_ENV = "production"
PORT = "5000"
